<h1>Planetary Systems</h1>

<% @planetary_systems.each do |system| %>
  <ul>
    <li><strong><%= system.name %></strong> Created at: <%= system.created_at %><%= link_to("Edit #{system.name}", "/planetary_systems/#{system.id}/edit") %></li>
  </ul>
<% end %> 

<%= link_to('New Planetary System', '/planetary_systems/new') %>